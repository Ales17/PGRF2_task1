package utils;

import model.Vectorizable;
import model.Vertex;

public class Lerp<V extends Vectorizable<V>> {
    Vertex lerp(Vertex a, Vertex b, double t) {
       //int x2 = (int) ((1-t) * v1 + t * v2);// TODO implement

        return null;
    }
}
